{
  "name": "assignment",
  "version": "1.0.0",
  "description": "You need to create an express HTTP server in Node.js which will handle the logic of a file server.\r   - Use built in Node.js `fs` module\r   The expected API endpoints are defined below,\r   1. GET /files - Returns a list of files present in `./files/` directory\r     Response: 200 OK with an array of file names in JSON format.\r     Example: GET http://localhost:3000/files\r   2. GET /file/:filename - Returns content of given file by name\r      Description: Use the filename from the request path parameter to read the file from `./files/` directory\r      Response: 200 OK with the file content as the response body if found, or 404 Not Found if not found. Should return `File not found` as text if file is not found\r      Example: GET http://localhost:3000/file/example.txt\r     - For any other route not defined in the server return 404\r     Testing the server - run `npm run test-fileServer` command in terminal\r const express = require('express');\r const fs = require('fs');\r const path = require('path');\r const app = express();",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "assignment": "file:",
    "express": "^4.21.2",
    "nodemon": "^3.1.9"
  }
}
